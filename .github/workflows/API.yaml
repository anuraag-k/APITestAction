name: HCL OneTest API

on: workflow_dispatch

jobs:
    API-Action:
        runs-on: self-hosted
        name: HCL OneTest API
        steps:
         - name: HCL OneTest API
           uses: anuraag-k/APITestAction@main
           with:
            projectDir:
            projectName: 
            environment: 
            tests: 
            parameterFile: C:\Users\anuraag.k\Downloads\api.txt
            junitDir: D:\reports
